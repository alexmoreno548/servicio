<p id="notice"><%= notice %></p>
	<h1><%= @notice.title %></h1>  
 	<%= image_tag @notice.image.url(:large) %>
	<p><%= @notice.body.html_safe %></p>
<%= link_to 'Edit', edit_notice_path(@notice) %> |
<%= link_to 'Back', notices_path %>
